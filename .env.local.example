# Convex (required for dashboard)
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
CONVEX_SITE_URL=https://your-deployment.convex.site

# Convex env vars (set in Convex dashboard for actions):
# AGENT_API_URL=http://localhost:8000  — to trigger real agent from Launch Real Attack
# FIXER_API_URL=http://localhost:8001  — for Rebuild Security to create real PRs

# Agent (agent/main.py): CONVEX_SITE_URL
# Fixer (fixer/api.py): CONVEX_SITE_URL, GITHUB_TOKEN, GEMINI_API_KEY
# GITHUB_REPO=jayadevgh/jayadevgh.github.io  # for testing static site fixes
